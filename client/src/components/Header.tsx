import React from "react";

const Header = (props: React.PropsWithChildren<{}>) => {
  return (
    <header className="page-header">
      <h2>Fleetman React</h2>
    </header>
  );
};

export default Header;
